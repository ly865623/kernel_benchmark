# Outline Revise

- Outline ID: `outline-001`
- Title: Stage-Centric Microbenchmark-Calibrated Latency Modeling of Sparse Attention Kernels on Blackwell GPUs
- Mode: `revise`
- Reason: Resolve the last unresolved required item (main-results result_table embed desync) and register the two-kernel generalization result. Restored the full compiled evidence_view (1 section, main-results) whose on-disk copy had been truncated to sections:[], re-embedding the 9 populated rows (A1-A8 + dsa-stagepred-grid-v2) so the required item clears; additionally appended one optional row dsa-stagepred-twokernel (cross-kernel generalization: small 4.83% / regular 7.39% / both 6.01% over 104 configs) matching the new manuscript Section "Generalization across the family's two dispatch kernels". No claim/number changes to existing rows; headline 4.92% unchanged.

## Note

Resolve the last unresolved required item (main-results result_table embed desync) and register the two-kernel generalization result. Restored the full compiled evidence_view (1 section, main-results) whose on-disk copy had been truncated to sections:[], re-embedding the 9 populated rows (A1-A8 + dsa-stagepred-grid-v2) so the required item clears; additionally appended one optional row dsa-stagepred-twokernel (cross-kernel generalization: small 4.83% / regular 7.39% / both 6.01% over 104 configs) matching the new manuscript Section "Generalization across the family's two dispatch kernels". No claim/number changes to existing rows; headline 4.92% unchanged.
